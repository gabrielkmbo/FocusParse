"""FocusParse — agentic high-resolution document parsing pipeline.

Consumes the `gabrielbo/parser-bench` benchmark (read-only git submodule at
`third_party/parser-bench/`) and emits citation-grounded answers + trajectory
traces for later OSS distillation.
"""

__version__ = "0.1.0"
