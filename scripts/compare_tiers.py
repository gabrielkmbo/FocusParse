"""Run the tier sweep for Phase 5: simple × 3 frontier models, focus × tier
profiles, focus + LlamaParse (optional). Emit a markdown + HTML table.

TODO(Phase 5): wire after FocusWorkflow lands.
"""

from __future__ import annotations


def main() -> None:
    raise NotImplementedError("compare_tiers — Phase 5")


if __name__ == "__main__":
    main()
